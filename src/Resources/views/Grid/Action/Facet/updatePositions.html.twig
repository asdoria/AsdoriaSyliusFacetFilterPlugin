<form class="asdoria-update-facets"
      action="{{ path('asdoria_admin_ajax_facet_update_position') }}"
      data-csrf-token="{{ csrf_token('update-facet-position') }}"
>
    <button class="btn" type="submit">
        {{ ux_icon('tabler:pencil', { 'class': 'icon icon-tabler' }) }}
        {{ 'sylius.ui.save_positions'|trans }}
    </button>
</form>
